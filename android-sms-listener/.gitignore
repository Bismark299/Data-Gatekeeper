# Build artifacts
/app/build/
/build/

# Local properties (contains SDK path, signing info)
local.properties

# Keystores - NEVER commit these
*.keystore
*.jks

# Release outputs already tracked elsewhere
/app/release/
